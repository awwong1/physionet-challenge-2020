{
  "exp_name": "PhysioNet2020/FeatureCNN",
  "agent": "agents.physionet2020.ClassificationAgent",
  "dataset": {
    "name": "datasets.physionet2020.PhysioNet2020Dataset",
    "kwargs": {
      "data_dir": "Training_WFDB",
      "max_seq_len": 4000,
      "ensure_equal_len": true
    }
  },
  "cross_validation": {
    "fold": 5,
    "val_offset": 0
  },
  "train_loader": {
      "batch_size": 128,
      "num_workers": 16,
      "shuffle": true,
      "pin_memory": true
  },
  "val_loader": {
      "batch_size": 128,
      "num_workers": 16,
      "shuffle": false,
      "pin_memory": true
  },
  "model": {
    "name": "models.feature_cnn_lstm.FeatureCNN"
  },
  "criterion": {
    "name": "torch.nn.BCEWithLogitsLoss"
  },
  "optimizer": {
    "name": "apex.optimizers.FusedAdam",
    "kwargs": {
      "lr": 0.01
    }
  },
  "scheduler": {
    "name": "torch.optim.lr_scheduler.StepLR",
    "kwargs": {
      "step_size": 18,
      "gamma": 0.1
    }
  },
  "epochs": 30,
  "log_per_num_batch": 1,
  "use_amp": true
}

